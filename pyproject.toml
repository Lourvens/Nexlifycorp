[project]
name = "nexlifycorp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.0.0",
    "sec-edgar-downloader>=5.0.0",
]
